import React from 'react';
import './PrimaryButtonLoad.scss';

export const PrimaryButtonLoad: React.FC = () => {
  return (
    <button
      className="primary-button primary-button--load"
    >
      Add to cart
    </button>
  );
};
