export interface PhoneMain {
  id: number,
  capacity: string,
  category: string,
  name: string,
  nameSpaceId: string,
  priceDiscount: number,
  priceRegular: number,
  ram: string,
  screen: string,
  slug: string,
}
